;; Count Leading Zeros Word (X-form, RC=0)
((operands
 ((rA Gprc)
  (rS Gprc)))
 (in
  (op.rS loc.IP))
 (defs
  ((op.rA
   (with
    ()
    (call uf.clz.32 op.rS)))
   (loc.IP
    (with
     ()
     (bvadd loc.IP #x00000004))))))
