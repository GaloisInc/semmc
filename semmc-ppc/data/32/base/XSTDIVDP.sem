;; VSX Scalar Test for Software Divide Double-Precision (XX3-form)
((operands
 ((bf Crrc)
  (xA Vsrc)
  (xB Vsrc)))
 (in
  (loc.CR op.xB op.xA op.bf loc.IP))
 (defs
  ((loc.CR
   (with
    ()
    (call uf.undefinedBV.32)))
   (loc.IP
    (with
     ()
     (bvadd loc.IP #x00000004))))))
