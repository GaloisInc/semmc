;; Load Vector for Shift Left Indexed (X-form)
((operands
 ((vrT . 'Vrrc)
  (memrr . 'Memrr)))
 (in
  ('Mem memrr 'IP))
 (defs
  ((vrT
   ((_ call "uf.undefined")
    #x00000080))
   ('IP
    (bvadd 'IP #x00000004)))))
