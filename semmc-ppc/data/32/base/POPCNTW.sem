;; Population Count Words (X-form)
((operands
 ((rA Gprc)
  (rS Gprc)))
 (in
  (op.rS loc.IP))
 (defs
  ((op.rA
   (with
    ()
    (call uf.popcnt.32 op.rS)))
   (loc.IP
    (with
     ()
     (bvadd loc.IP #x00000004))))))
