;; VSX Splat Word (XX2-form)
((operands
 ((xT Vsrc)
  (UIM U2imm)
  (xB Vsrc)))
 (in
  (op.xB op.UIM loc.IP))
 (defs
  ((op.xT
   (with
    ()
    (call uf.undefinedBV.128)))
   (loc.IP
    (with
     ()
     (bvadd loc.IP #x00000004))))))
