;; Load Vector for Shift Right Indexed (X-form)
((operands
 ((vrT Vrrc)
  (memrr Memrr)))
 (in
  (loc.Mem op.memrr loc.IP))
 (defs
  ((op.vrT
   (with
    ()
    (call uf.undefinedBV.128)))
   (loc.IP
    (with
     ()
     (bvadd loc.IP #x00000004))))))
