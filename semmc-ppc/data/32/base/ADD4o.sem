;; ADD. (XO-form, RC=1)
((operands
 ((rT . 'Gprc)
  (rA . 'Gprc)
  (rB . 'Gprc)))
 (in
  ('XER 'CR rB rA 'IP))
 (defs
  (('CR
   ((_ call "df.cmpImm")
    (bvslt
     (bvadd rA rB)
     #x00000000)
    (bvsgt
     (bvadd rA rB)
     #x00000000)
    #b000
    'XER
    'CR))
   (rT
    (bvadd rA rB))
   ('IP
    (bvadd 'IP #x00000004)))))
