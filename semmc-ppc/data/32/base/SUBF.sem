;; SUBF (XO-form, RC=0)
((operands
 ((rT Gprc)
  (rB Gprc)
  (rA Gprc)))
 (in
  (op.rA op.rB loc.IP))
 (defs
  ((op.rT
   (with
    ()
    (bvsub op.rB op.rA)))
   (loc.IP
    (with
     ()
     (bvadd loc.IP #x00000004))))))
