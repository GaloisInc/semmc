;; NAND (X-form, RC=1)
((operands
 ((rA . 'Gprc)
  (rB . 'Gprc)
  (rS . 'Gprc)))
 (in
  ('XER 'CR rB rS 'IP))
 (defs
  (('CR
   ((_ call "df.cmpImm")
    (bvslt
     (bvnot
      (bvand rS rB))
     #x00000000)
    (bvsgt
     (bvnot
      (bvand rS rB))
     #x00000000)
    #b000
    'XER
    'CR))
   (rA
    (bvnot
     (bvand rS rB)))
   ('IP
    (bvadd 'IP #x00000004)))))
