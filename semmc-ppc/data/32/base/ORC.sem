;; OR with Complement (X-form, RC=0)
((operands
 ((rA . 'Gprc)
  (rB . 'Gprc)
  (rS . 'Gprc)))
 (in
  (rB rS 'IP))
 (defs
  ((rA
   (bvor
    rS
    (bvnot rB)))
   ('IP
    (bvadd 'IP #x00000004)))))
