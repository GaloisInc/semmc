;; BCCTR (XL-form, LK=1)
;; This variant has BO=01100, branch when the CR bit is 1
((operands
 ((bi . 'Crbitrc)))
 (in
  ('CR 'IP 'CTR))
 (defs
  (('LNK
   (bvadd 'IP #x00000004))
   ('IP
    (ite
     ((_ call "uf.test_bit_dynamic.32")
      'CR
      ((_ zero_extend 27)
       (bvsub #b11111 bi)))
     (concat
      ((_ extract 31 2)
       'CTR)
      #b00)
     (bvadd 'IP #x00000004))))))
