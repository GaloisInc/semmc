;; BCLR (XL-form, LK=0)
((operands
 ((bi . 'Crbitrc)))
 (in
  ('CR 'IP 'LNK))
 (defs
  (('IP
   (ite
    ((_ call "test_bit_dynamic")
     'CR
     ((_ zero_extend 27)
      (bvsub #b11111 bi)))
    (concat
     ((_ extract 29 0)
      'LNK)
     #b00)
    (bvadd 'IP #x00000004))))))
