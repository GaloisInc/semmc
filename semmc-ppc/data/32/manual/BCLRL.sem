;; BCLRL (XL-form, LK=1)
((operands
 ((bi . 'Crbitrc)))
 (in
  ('CR 'IP 'LNK))
 (defs
  (('LNK
   (bvadd 'IP #x00000004))
   ('IP
    (ite
     ((_ call "uf.test_bit_dynamic.32")
      'CR
      ((_ zero_extend 27)
       (bvsub #b11111 bi)))
     (concat
      ((_ extract 31 2)
       'LNK)
      #b00)
     (bvadd 'IP #x00000004))))))
