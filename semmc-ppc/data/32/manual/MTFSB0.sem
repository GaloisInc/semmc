;; Move to FPSCR Bit 0 (X-form, RC=0)
((operands
 ((BT U5imm)))
 (in
  (loc.IP))
 (defs
  ((loc.FPSCR
   (with
    ()
    (call uf.undefinedBV.32)))
   (loc.IP
    (with
     ()
     (bvadd loc.IP #x00000004))))))
