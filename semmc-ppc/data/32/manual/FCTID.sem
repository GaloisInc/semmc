;; Floating Point Convert to Integer Doubleword (X-form)
((operands
 ((frT . 'Fprc)
  (frB . 'Fprc)))
 (in
  ('FPSCR frB 'IP))
 (defs
  (('FPSCR
   ((_ extract 31 0)
    ((_ call "ppc.vec1")
     "FCTID"
     frB
     'FPSCR)))
   (frT
    ((_ extract 159 32)
     ((_ call "ppc.vec1")
      "FCTID"
      frB
      'FPSCR)))
   ('IP
    (bvadd 'IP #x00000004)))))
