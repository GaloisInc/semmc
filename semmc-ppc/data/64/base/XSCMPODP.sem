;; VSX Scalar Compare Ordered Double-Precision (XX3-form)
((operands
 ((bf Crrc)
  (xA Vsrc)
  (xB Vsrc)))
 (in
  (op.bf loc.CR op.xB op.xA loc.IP))
 (defs
  ((loc.CR
   (with
    ()
    (call uf.undefinedBV.32)))
   (loc.IP
    (with
     ()
     (bvadd loc.IP #x0000000000000004))))))
