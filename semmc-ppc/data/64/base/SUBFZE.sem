;; Subtract From Zero Extended (XO-form, RC=0)
((operands
 ((rT. 'Gprc)
  (rA. 'Gprc)))
 (in
  ('XER rA 'IP))
 (defs
  ((rT
   (bvadd
    (bvnot rA)
    ((_ zero_extend 63)
     ((_ extract 34 34)
      'XER))))
   ('IP
    (bvadd 'IP #x0000000000000004)))))
