;; Move To VSR Doubleword (XX1-form)
((operands
 ((xT Vsrc)
  (rA Gprc)))
 (in
  (op.xT loc.IP))
 (defs
  ((op.xT
   (with
    ()
    (concat
     op.rA
     (call uf.undefinedBV.64))))
   (loc.IP
    (with
     ()
     (bvadd loc.IP #x0000000000000004))))))
