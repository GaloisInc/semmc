;; Move To VSR Word Zero (XX1-form)
((operands
 ((xT . 'Vsrc)
  (rA . 'Gprc)))
 (in
  (xT 'IP))
 (defs
  ((xT
   (concat
    ((_ zero_extend 32)
     ((_ extract 63 32)
      rA))
    ((_ call "undefined")
     #x00000040)))
   ('IP
    (bvadd 'IP #x0000000000000004)))))
