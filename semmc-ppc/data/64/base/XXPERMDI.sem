;; VSX Permute Doubleword Immediate (XX3-form)
((operands
 ((xT Vsrc)
  (DM U2imm)
  (xA Vsrc)
  (xB Vsrc)))
 (in
  (op.xB op.xA op.DM loc.IP))
 (defs
  ((op.xT
   (with
    ()
    (call uf.undefinedBV.128)))
   (loc.IP
    (with
     ()
     (bvadd loc.IP #x0000000000000004))))))
