;; Vector Maximum Unsigned Word (VX-form)
((operands
 ((vrT . 'Vrrc)
  (vrA . 'Vrrc)
  (vrB . 'Vrrc)))
 (in
  ('VSCR vrB vrA 'IP))
 (defs
  (('VSCR
   ((_ extract 31 0)
    ((_ call "ppc.vec2")
     "VMAXUW"
     vrA
     vrB
     'VSCR)))
   (vrT
    ((_ extract 159 32)
     ((_ call "ppc.vec2")
      "VMAXUW"
      vrA
      vrB
      'VSCR)))
   ('IP
    (bvadd 'IP #x0000000000000004)))))
