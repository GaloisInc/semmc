;; Branch (I-form, AA=0, LK=0)
((operands
 ((target Directbrtarget)))
 (in
  (loc.IP op.target))
 (defs
  ((loc.IP
   (with
    ()
    (bvadd
     loc.IP
     ((_ sign_extend 38)
      (concat op.target #b00))))))))
