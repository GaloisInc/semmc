((operands
 ((rA Gprc)
  (imm S16imm)))
 (in
  (op.imm loc.IP))
 (defs
  ((op.rA
   (with
    ()
    ((_ sign_extend 48)
     op.imm)))
   (loc.IP
    (with
     ()
     (bvadd loc.IP #x0000000000000004))))))
