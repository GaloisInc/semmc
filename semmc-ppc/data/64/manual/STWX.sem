;; Store Word Indexed (X-form)
((operands
 ((memref . 'Memrr)
  (rS . 'Gprc)))
 (in
  ('Mem memref rS 'IP))
 (defs
  (('Mem
   ((_ call "write_mem.32")
    'Mem
    (bvadd
     (ite
      ((_ call "ppc.is_r0")
       ((_ call "ppc.memrr_base")
        memref))
      #x0000000000000000
      ((_ call "ppc.memrr_base")
       memref))
     ((_ call "ppc.memrr_offset")
      memref))
    ((_ extract 63 32)
     rS)))
   ('IP
    (bvadd 'IP #x0000000000000004)))))
