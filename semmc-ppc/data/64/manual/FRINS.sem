;; Floating Round to Integer Nearest Single (X-form)
((operands
 ((frT . 'Fprc)
  (frB . 'Fprc)))
 (in
  ('FPSCR frB 'IP))
 (defs
  (('FPSCR
   ((_ extract 31 0)
    ((_ call "ppc.vec1")
     "FRINS"
     frB
     'FPSCR)))
   (frT
    ((_ extract 159 32)
     ((_ call "ppc.vec1")
      "FRINS"
      frB
      'FPSCR)))
   ('IP
    (bvadd 'IP #x0000000000000004)))))
