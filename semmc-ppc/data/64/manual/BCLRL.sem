;; BCLR (XL-form, LK=1)
((operands
 ((bi. 'Crbitrc)))
 (in
  ('CR 'IP 'LNK))
 (defs
  (('LNK
   (bvadd 'IP #x0000000000000004))
   ('IP
    (ite
     (notp
      ((_ call "test_bit_dynamic")
       ((_ zero_extend 27)
        bi)
       'CR))
     (concat
      ((_ extract 61 0)
       'LNK)
      #b00)
     (bvadd 'IP #x0000000000000004))))))
