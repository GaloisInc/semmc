;; BCLR (XL-form, LK=1)
((operands
 ((bi . 'Crbitrc)))
 (in
  ('CR 'IP 'LNK))
 (defs
  (('LNK
   (bvadd 'IP #x0000000000000004))
   ('IP
    (ite
     ((_ call "test_bit_dynamic")
      'CR
      ((_ zero_extend 27)
       (bvsub #b11111 bi)))
     (concat
      ((_ extract 61 0)
       'LNK)
      #b00)
     (bvadd 'IP #x0000000000000004))))))
